extends layout

block content
  h1= title
  p Welcome to #{title}

  form(name="input", action='api/Products/:id', method='get')
   input(type='text', name="name", placeholder="name") 
   br
   br
   input(type='text', name="description", placeholder="description")
   br
   br
   input(type='submit', value='Update Product')